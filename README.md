# Web_Scraping
Web scarping of amazon dataset of headset and takes it's details
#if in cause not working after downloading the code by zip run the codes given in the terminal/bash.
pip install requests
#If you're using Python 3, you might need to use pip3 instead:
pip3 install requests
pip install beautifulsoup4
#If you're using Python 3, you might need to use pip3 instead:
pip3 install beautifulsoup4
